"""Search service — full-text search utilities."""

from __future__ import annotations

# This module is a placeholder for future PostgreSQL full-text search
# and pgvector semantic search integration.
#
# For Phase 2, the search API uses simple ILIKE queries.
# Phase 3 will add:
# - PostgreSQL tsvector columns with GIN indexes
# - ts_rank for relevance scoring
# - pgvector embeddings for semantic search
